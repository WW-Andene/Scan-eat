<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Base theme delegates to Compose; window background = OLED black.
         Must descend from Theme.AppCompat (not a plain platform theme) —
         MainActivity is an AppCompatActivity, required for per-app language
         switching (AppCompatDelegate) to actually apply on API 26-32. -->
    <style name="Theme.ScanEat" parent="Theme.AppCompat.NoActionBar">
        <item name="android:windowBackground">@android:color/black</item>
        <item name="android:statusBarColor">@android:color/black</item>
        <item name="android:navigationBarColor">@android:color/black</item>
        <item name="android:windowLightStatusBar">false</item>
    </style>

    <!-- Cold-start splash theme. Swapped to Theme.ScanEat by installSplashScreen()
         once SplashViewModel finishes its DataStore reads. -->
    <style name="Theme.ScanEat.Starting" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">@android:color/black</item>
        <item name="windowSplashScreenAnimatedIcon">@drawable/ic_launcher_foreground</item>
        <item name="postSplashScreenTheme">@style/Theme.ScanEat</item>
    </style>
</resources>
