# Copy this to .env and fill in values
# .env is gitignored — never commit your key

# Optional: if set, used as the server's Groq key for all requests
# that don't pass their own X-Groq-Key header.
GROQ_API_KEY=gsk_...

# Server port (default 8080)
PORT=8080
